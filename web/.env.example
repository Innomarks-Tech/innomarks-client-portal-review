# Intake stays disabled until the privacy notice, staff access and end-to-end tests are approved.
INQUIRIES_ENABLED=false
NOTIFICATIONS_ENABLED=false
SITE_URL=http://localhost:3000
SUPABASE_URL=
# Server-only. Never prefix privileged credentials with NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=
RATE_LIMIT_SECRET=
RESEND_API_KEY=
# Must be a verified sender. The recipient is fixed in server code.
RESEND_FROM=
